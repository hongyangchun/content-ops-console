name = "content-ops-sync"
main = "index.js"
compatibility_date = "2025-04-01"

# 跨设备同步用的 KV 命名空间。
# 首次部署前请先执行：wrangler kv namespace create SYNC_KV
# 把输出的 id 填到下面的 id 字段。
[[kv_namespaces]]
binding = "SYNC_KV"
id = "REPLACE_WITH_KV_ID"
